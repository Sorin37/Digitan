using System.Collections;
using System.Collections.Generic;
using Unity.Netcode;
using UnityEngine;

public class TestandChestii : NetworkBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.LogError("sal");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
